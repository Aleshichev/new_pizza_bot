version: '3'

services:
  postgres:
    image: postgres:latest
    container_name: postgres_pizza
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=admin
      - POSTGRES_DB=new_pizza_db
